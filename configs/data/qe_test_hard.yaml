# This config implements a task highly unlikely to transfer well to the data store
name: COCO
kwargs:
  split: testARC
  embedding: dinov3
  # positive_class:
  # - cell phone
  # negative_class:
  # - person
  # min_bbox_area_fraction: 0.01
  # max_bbox_area_fraction: 0.5
  positive_class:
  - cat
  negative_superclass:
  - vehicle
  max_objects_per_image: 1
  filter_class: positive
